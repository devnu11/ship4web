export interface Event {
  id: number;
  title: string;
  date: string;
  time: string;
  location: string;
}

export interface ImagePlaceholder {
  id: number;
  title: string;
  description: string;
}